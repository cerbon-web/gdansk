# gdansk-backend

Simple Node.js backend for the Mosque of Gdansk project.

## Setup

Open PowerShell and run:

```powershell
cd backend
npm install
npm start
```

- Server runs on port `3000` by default.
- Endpoint: `GET /test` returns JSON with current time and Node version.
